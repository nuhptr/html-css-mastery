const person = {
  name: "Eko",
  sayHello: function (name) {
    console.info(`Hello ${name}`);
  },
};

// person.sayGoodBye = function (name) {
//   `See you ${name}`;
// };

// person.sayGoodBye("Adi");

person.sayHello("Adi");
