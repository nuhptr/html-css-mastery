const mantanTerindah = ["Adi", "Panji", "Dendi"]

// melooping data array
for (let index = 0; index < mantanTerindah.length; index++) {
    const element = mantanTerindah[index];
    console.log(element)
}

console.log("------------------------");

let index = 0
while (index < mantanTerindah.length) {
    const element = mantanTerindah[index]
    console.log(element)
    index++
}